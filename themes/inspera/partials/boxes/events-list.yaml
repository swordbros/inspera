name: Upcoming events
handle: events
labelFrom: title
section: Content
# translatable:

form:
  tabs:
    defaultTab: Content
    fields:

      box_title:
        label: box.box_title.label
        comment: box.box_title.comment
        default: box.image.name
      
      title-mixin:
        type: mixin
      
      anchor-mixin:
        type: mixin

      # component brings events list, use component on yaml config
      # upcoming_events:
      #   type: repeater
      #   promt: Item
      #   label: Upcoming events item
      #   form:
      #     fields:
      #       item_title:
      #         label: Event title
      #       item_date:
      #         label: Event date
      #       pic:
      #         type: mediafinder
      #         mode: image
      #         label:  Picture
      #       pic_title: 
      #         label: Alt title for CEO


            


     

    
