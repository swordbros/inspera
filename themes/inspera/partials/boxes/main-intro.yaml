name: box.intro.name
handle: intro
labelFrom: title
section: box.accordion.defaultTab

form:
  tabs:
    defaultTab: box.accordion.defaultTab
    fields:
      box_title:
        label: box.box_title.label
        comment: box.box_title.comment
        default: box.art_inspiration.name
        
      slides:
        type: repeater
        prompt: box.intro.name.slides.prompt 
        label: box.intro.name.slides.label 
        form:
          fields:
            title-mixin:
              type: mixin
            background-mixin:
              type: mixin
              tab: box.mixin_background.tab
            text:
              label: box.intro.name.slides.text.label 
              type: richeditor
            # pic:
            #   type: mediafinder
            #   mode: image
            #   label: box.intro.name.slides.pic.label 
            # pic_mobile:
            #   type: mediafinder
            #   mode: image
            #   label: box.intro.name.slides.pic_mobile.label 
            # pic_title: 
            #   label: box.intro.name.slides.pic_title.label

      anchor-mixin:
        type: mixin


            


     

    
