name: CTA
handle: cta
labelFrom: box_title
section: box.sections.sections

form:
  tabs:
    defaultTab: box.defaultTab.content
    fields:
      box_title:
        label: box.box_title.label
        comment: box.box_title.comment
        default: CTA
      
      title-mixin:
        type: mixin
      
      button-mixin:
        type: mixin
      
      background-mixin:
        type: mixin
        tab: box.mixin_background.tab
      
      min_height:
        label: box.cta.min_height.label
        type: number

      no-bottom-spacing-mixin:
        type: mixin
      
      anchor-mixin:
        type: mixin
      
      classes-mixin:
        type: mixin