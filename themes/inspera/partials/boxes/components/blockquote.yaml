name: box.blockquote.name
handle: nested-blockquote
labelFrom: box_title
section: box.section.base_components
contexts: 
  - nested-boxes
  - blog
validation:
  rules:
    text:
      - required
form:
  tabs:
    defaultTab: box.text.defaultTab
    fields:

      box_title:
        label: box.box_title.label
        comment: box.box_title.comment
        default: box.blockquote.name
        
      text:
        label: box.text.fields.text.label
        type: textarea
      
      classes-mixin:
        type: mixin

      margin-mixin:
        type: mixin

      anchor-mixin:
        type: mixin