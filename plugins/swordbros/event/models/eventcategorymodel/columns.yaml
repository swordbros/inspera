columns:
    thumb:
        label: event.cols.thumb
        type: image
        mode: image
    id:
        label: id
        type: event.cols.id
        invisible: true
    code:
        label: event.cols.code
        type: text
        invisible: true
    event_type_id:
        label: event.cols.event_type
        #relation: event_type
        #select: name
        type: text
    name:
        label: event.cols.name
        type: text
    description:
        label: event.cols.description
        type: text
    status:
        label: event.cols.status
        type: switch
    deleted_at:
        label: event.cols.deleted_at
        type: datetime
        invisible: true
    created_at:
        label: event.cols.created_at
        type: datetime
        invisible: true
    updated_at:
        label: event.cols.updated_at
        type: datetime
        invisible: true
