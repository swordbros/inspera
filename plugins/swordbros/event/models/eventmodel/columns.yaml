columns:
    thumb:
        label: event.cols.thumb
        type: image
        mode: image
    id:
        label: event.cols.id
        type: number
        invisible: true
        sortable: true
        sortableDefault: DESC
    group_key:
        label: event.cols.group_key
        type: text
        invisible: true
    title:
        label: event.cols.title
        type: text
    event_zone_id:
        label: event.cols.event_zone
        relation: event_zone
        select: name
    capacity:
        label: event.cols.capacity
        type: partial
        path: capacity
    rating:
        label: event.cols.rating
        type: partial
        path: rating
    event_category_id:
        label: event.cols.event_category
        relation: event_category
        select: name
    event_type_id:
        label: event.cols.event_type
        relation: event_type
        select: name
    status:
        label: event.cols.status
        type: switch
    deleted_at:
        label: event.cols.deleted_at
        type: datetime
        invisible: true
    created_at:
        label: event.cols.created_at
        type: datetime
        invisible: true
    updated_at:
        label: event.cols.updated_at
        type: datetime
        invisible: true
