columns:
    thumb:
        label: event.cols.thumb
        type: image
    id:
        label: event.cols.id
        type: number
        invisible: true
    code:
        label: event.cols.code
        type: text
        invisible: true
    name:
        label: event.cols.name
        type: text
        searchable: true
    description:
        label: event.cols.description
        type: text
    capacity:
        label: event.cols.capacity
        type: number
    status:
        label: event.cols.status
        type: switch
    deleted_at:
        label: event.cols.deleted_at
        type: datetime
        invisible: true
    created_at:
        label: event.cols.created_at
        type: datetime
        invisible: true
    updated_at:
        label: event.cols.updated_at
        type: datetime
        invisible: true
