# ===================================
# Filter Scope Definitions
# ===================================

scopes:
    tag:
        label: event.scops.tag
        scope: isTag
        modelClass: Swordbros\Event\Models\EventTagModel
        conditions: filterByTag
        options: getTagOptions

