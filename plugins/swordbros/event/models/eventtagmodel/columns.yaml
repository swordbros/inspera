columns:
    id:
        label: event.cols.id
        type: number
        invisible: true

    event_id:
        label: event.cols.event_zone
        relation: event
        select: title
    tag:
        label: event.cols.tag
        type: selectable

