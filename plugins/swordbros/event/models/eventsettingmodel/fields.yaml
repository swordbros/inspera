tabs:
    defaultTab:  event.tab.main
    fields:
        alert_emails:
            label: event.fields.alert_emails
            comment:  You can separate emails with ";"
            type: email
            required: true
            tab: event.tab.email_settings


